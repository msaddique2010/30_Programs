// 29.	Write a program to print a message if the value of variable "n" is greater than 100, otherwise ignore the statement. 
#include <stdio.h>

void program29() {
    printf("29.	Write a program to print a message if the value of variable \"n\" is greater than 100, otherwise ignore the statement.\n");
    // input
    int n = 101;

    // Processing
    if (n > 100) {
        printf("Number is greater than 100\n"); // Output
    }

    // return 0;
}
