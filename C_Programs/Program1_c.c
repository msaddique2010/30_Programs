// 1. Write a program to assigned values to three variables at the time of declaration. Print the assigned values on the computer screen. 
#include <stdio.h>
#include <conio.h>

void program1(){
    printf("1. Write a program to assigned values to three variables at the time of declaration. Print the assigned values on the computer screen. \n");
    // Processing
    int a = 12;
    float b = 3.5;
    char c = 'A';

    // Output
    printf("%d\n", a);
    printf("%.2f\n", b);
    printf("%c\n", c);
    // return 0;
}
